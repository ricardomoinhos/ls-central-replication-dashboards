# LS Central Telemetry Infrastructure

This folder contains the telemetry messaging infrastructure used by the extension to emit custom telemetry events to Application Insights. It provides the interfaces and base implementations for sending structured telemetry messages.

> **Note:** These objects are a temporary implementation. They will be replaced by the official LS Retail Telemetry app once it is made available.